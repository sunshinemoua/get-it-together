// import React from "react";

// const CompletedTodos = ({ todos, checkedTodo }) => {
//   const checkedTodos = todos.map((todo) => (
//     <div key={Math.random()} onClick={() => checkedTodo}>
//       {todo.completed && (
//         <ul>
//           <li>
//             {todo.todo}
//             {todo.date.toString()}
//           </li>
//         </ul>
//       )}
//     </div>
//   ));
//   return <div>{checkedTodos}</div>;
// };

// export default CompletedTodos;
